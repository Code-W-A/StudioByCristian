import type { Metadata } from "next"
export const metadata: Metadata = { title: "Cookie Policy | Studio ByCristian", description: "Cookie categories and consent controls used by Studio ByCristian.", alternates: { canonical: "/cookie-policy" } }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
